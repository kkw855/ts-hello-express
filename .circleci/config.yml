version: 2.1
jobs:
  test:
    machine:
      image: cimg/node:12.18.3
    working_directory: ~/ts-hello-express
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            npm install
      - run:
          name: Run tests
          command: |
            npm run test

  build:
    machine:
      image: cimg/node:12.18.3
    working_directory: ~/ts-hello-express
    steps:
      - checkout
      - run:
          name: Build
          command: |
            npm run build

workflows:
  version: 2.1
  build-deploy:
    jobs:
      - test:
          filters:
            branches:
              only: master
      - build:
          requires:
            - test
          filters:
            branches:
              only: master

