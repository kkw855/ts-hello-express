version: 2.17
jobs:
  test:
    docker:
      - image: cimg/node:12.16
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            npm install
      - run:
          name: Run tests
          command: |
            npm run test


