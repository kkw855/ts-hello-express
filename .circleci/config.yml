version: 2.17
jobs:
  test:
    working_directory: ~/hello-sts
    docker:
      - immage: cimg/node:12.16
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            npm install
      - run:
          name: Run tests
          command: |
            npm run test

workflows:
  version: 2
  build-deploy:
    jobs:
      - test:
          filters:
            branches:
              only: master

